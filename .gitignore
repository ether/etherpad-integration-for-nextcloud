.DS_Store
vendor/
node_modules/
.phpunit.cache/
_copy_probe/
dist/

# Playwright E2E (tests/e2e)
tests/e2e/.env.e2e
tests/e2e/.auth/
test-results/
playwright-report/
blob-report/
.playwright/

# Generated by tests/e2e/docker/up.sh — throwaway credentials and a
# per-run CA. Ignored here rather than only next to the generator, so a
# branch that does not carry the generator cannot commit them by accident.
tests/e2e/.env.e2e.docker
tests/e2e/docker/certs/
